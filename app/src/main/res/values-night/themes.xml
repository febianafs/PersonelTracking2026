<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.PersonelTracking2026" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="popupMenuStyle">@style/DarkPopupMenu</item>
        <item name="android:popupMenuStyle">@style/DarkPopupMenu</item>
    </style>

    <style name="Theme.PersonelTracking2026" parent="Base.Theme.PersonelTracking2026" />

    <style name="Widget.App.PrimaryButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/btn_primary_selector</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="DarkAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="android:background">@color/background</item>
        <item name="android:textColor">@color/white</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="colorSurface">@color/background</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

</resources>